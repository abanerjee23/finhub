[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "uv sync",
  "npm --prefix frontend ci"
]

[phases.build]
cmds = ["npm --prefix frontend run build"]

[start]
cmd = "uv run cfin-api"
